import React from 'react';

// Header Components
import Nav from "./Nav";
import SearchForm from "./SearchForm";

const Header = () => {
  return(
    <div>
      <Nav />
      <SearchForm />
    </div>
  );
}

export default Header;
